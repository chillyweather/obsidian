## Converting Letter Case with the `toLowerCase()` and `toUpperCase()` Methods

```js
"Turn Caps Lock on".toLowerCase(); // "turn caps lock on" 
"Turn Caps Lock off".toUpperCase(); // "TURN CAPS LOCK OFF"
```